{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es5", /* Define a versão ES5 para compatibilidade mais ampla */
    "module": "commonjs", /* Usa o sistema de módulos CommonJS (compatível com Node.js e muitos ambientes) */
    "outDir": "./dist/js", /* Diretório de saída para os arquivos JS compilados */
    "rootDir": "./src/js", /* Diretório de origem para os arquivos TypeScript */
    "strict": true, /* Ativa todas as verificações de tipo estritas */
    "esModuleInterop": true, /* Facilita a interoperabilidade entre CommonJS e ES Modules */
    "skipLibCheck": true, /* Evita a verificação de arquivos de definição de tipos (.d.ts) */
    "forceConsistentCasingInFileNames": true /* Garante a consistência de maiúsculas e minúsculas nos nomes de arquivos */
  },
  "include": [
    "src/js/*.ts"
  ], /* Inclui todos os arquivos TypeScript da pasta src/js */
  "exclude": [
    "node_modules"
  ] /* Exclui a pasta node_modules */
}